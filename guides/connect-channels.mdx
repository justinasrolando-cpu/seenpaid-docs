---
title: "Connect your channels"
description: "Link the social accounts seenpaid will publish to and attribute."
---

A **channel** is a social account seenpaid can publish to. Connect one to start; connect several to cross-post and compare which network actually drives revenue.

## Connect an account

<Steps>
  <Step title="Open Connections">
    In the dashboard, go to **Connections**.
  </Step>
  <Step title="Pick a platform">
    Choose the network you want to link and click **Connect**. You'll be sent to that platform to authorize seenpaid, then returned to the dashboard.
  </Step>
  <Step title="Confirm it's active">
    The account appears in your channel list with an **active** status. It's now available in Compose and to your AI agent.
  </Step>
</Steps>

## Available platforms

seenpaid publishes to **25 channels** in total. How you connect one depends on what that platform asks of a scheduler:

<CardGroup cols={3}>
  <Card title="Directly connectable" icon="circle-check">
    LinkedIn, Bluesky, Mastodon, Pinterest, and the API-based publishing targets — click **Connect** and authorize.
  </Card>
  <Card title="Bring your own keys" icon="key">
    X and other networks that gate API access behind their own developer app. Paste your credentials once.
  </Card>
  <Card title="Pending review" icon="clock">
    Facebook, Instagram, and Threads route through Meta's Graph API and are awaiting app review.
  </Card>
</CardGroup>

<Note>
  For the full list of channels with their caption limits, media rules, and per-platform catches, see [Supported platforms](/guides/platforms). The dashboard tells you which connect path a given platform needs when you open **Connections**.
</Note>

## Bring your own API keys (BYOK)

Some platforms — notably **X** — require their own API access on paid tiers. Where a platform needs your own credentials, seenpaid supports **bring-your-own-keys**: register a developer app on that platform, paste the keys once in Connections, and seenpaid publishes on your behalf.

## Troubleshooting

<AccordionGroup>
  <Accordion title="My account shows as disconnected">
    Access tokens can expire or be revoked by the platform. Open **Connections**, find the account, and click **Reconnect** to re-authorize.
  </Accordion>
  <Accordion title="A platform isn't letting me connect">
    A few networks require you to register a developer app on their side first. If the dashboard prompts for API keys, follow the linked steps for that platform, then return and connect.
  </Accordion>
</AccordionGroup>

<Card title="Next: connect Stripe" icon="stripe" href="/guides/connect-stripe">
  Channels handle publishing. Stripe is what turns clicks into attributed revenue.
</Card>
